# Updated LIBRARY_ROOT and paths

LIBRARY_ROOT = r"L:\3D_Library\CORT"
CORT_ROOT = os.path.join(LIBRARY_ROOT, "CORT_Specific")  # Update this with the correct subdirectory for CORT
THUMB_ROOT = os.path.join(LIBRARY_ROOT, "Thumbs")  # Update this with the correct subdirectory for Thumbs
NOTES_ROOT = os.path.join(LIBRARY_ROOT, "Notes")  # Update this with the correct subdirectory for Notes
